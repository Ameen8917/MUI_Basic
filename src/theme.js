import { createTheme } from "@mui/material";


export const theme = createTheme({
    palette: {
        primary: {
            main: "#6400Ff",
            light: "skyblue"
        },
        secodnary: {
            main: "blue",
        },
        otherColor: {
            main: "green"
        }
    }
})